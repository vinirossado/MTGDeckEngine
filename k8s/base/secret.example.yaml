# Example only — DO NOT commit real keys.
# In production, source these from AWS Secrets Manager or SSM Parameter Store
# via the External Secrets Operator, or kubectl create secret on a sealed-secret
# / SOPS-encrypted file.
apiVersion: v1
kind: Secret
metadata:
  name: mtg-deck-engine
  namespace: mtg-deck-engine
type: Opaque
stringData:
  TopDeck__ApiKey:     "REPLACE_WITH_TOPDECK_KEY"
  Ai__AnthropicApiKey: "REPLACE_WITH_ANTHROPIC_KEY"
