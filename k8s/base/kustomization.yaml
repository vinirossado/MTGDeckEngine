apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mtg-deck-engine
resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - cronjob-ingest.yaml
  # - secret.example.yaml          # generate the real Secret out-of-band
commonLabels:
  app.kubernetes.io/part-of: mtg-deck-engine
